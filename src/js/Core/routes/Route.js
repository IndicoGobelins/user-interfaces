export default class Route {
    /**
     *
     * @param {string} event
     * @param {Activity} activity
     */
    constructor(event, activity) {
        this.event = event;
        this.activity = activity;
    }
}